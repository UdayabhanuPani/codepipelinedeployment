version: 0.1
phase:
  install:
    commands:
      - aws cloudformation package --template-file SAM_template.yml --s3-bucket lambda-pipeline --output-template-file outputsamtemplate.yml
artifacts:
  type: zip
  files:
    -SAM_template.yml
    -outputsamtemplate.yml